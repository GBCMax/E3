# .NET microservices using REST API pattern, cotainers on dokerhub, Kubernetes cluster, synchronous messaging between services (HTTP & gRPC), asynchronous messaging between services using event bus (RabbitMQ).
+ [Architecture](ссылка)
